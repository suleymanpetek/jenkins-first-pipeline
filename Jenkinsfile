pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Jenkinsfile ile calisan pipeline"
                sh 'echo GitHub reposundan geldi'
            }
        }
    }
}
